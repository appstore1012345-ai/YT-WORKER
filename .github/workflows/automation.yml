name: Script Automation

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Script
        run: |
          python scripts/script_generator.py
          echo "Script run completed successfully ✅"

# Artifact upload step comment out kiya gaya hai taaki mobile me red error na aaye
#      - name: Upload generated script
#        uses: actions/upload-artifact@v3
#        with:
#          name: generated-script
#          path: scripts/script_generator.py
